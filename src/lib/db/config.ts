export const SUPABASE_CAMPAIGN_BASE_URL =
	process.env.NEXT_PUBLIC_SUPABASE_IMAGE_BASE_URL!;
// Private hostname for server-side operations (not exposed to client)
export const SUPABASE_HOSTNAME = process.env.SUPABASE_HOSTNAME!;
